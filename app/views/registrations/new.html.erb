<%= form_for @user, url: signup_path do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <label for="due" class="form-control, col-sm-2 control-label">Email</label>
      <div class="col-sm-10">
        <%= f.email_field :email, type:"email", class:"form-control, col-sm-5" %>
      </div>
    </div>

    <div class="form-group">
      <label for="due" class="form-control, col-sm-2 control-label">Password</label>
      <div class="col-sm-10">
        <%= f.password_field :password, type:"password", class:"form-control, col-sm-5" %>
      </div>
    </div>

    <div class="form-group">
      <label for="due" class="form-control, col-sm-2 control-label">Password Confirmation</label>
      <div class="col-sm-10">
        <%= f.password_field :password_confirmation, type:"password", class:"form-control, col-sm-5" %>
      </div>
    </div>

    <br></br>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit "Sign up" %>
      </div>
    </div>
  <br></br>


  </form>

<% end %>
