<% if model.errors.any? %>

      <div class="alert alert-danger">
        <h5><%= pluralize(model.errors.count, "error") %> prohibited this from being saved:</h5>
        <ul>
        <% model.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </div>

      </ul>
<% end %>
