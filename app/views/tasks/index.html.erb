<h1>Tasks<%= link_to 'Create Task', new_task_path, class:"btn btn-info btn-large, col-md-offset-8" %></h1>
<hr></hr>
<table class="table">
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="1">Complete</th>
      <th colspan="3">Due Date</th>
    </tr>
  </thead>


  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.description%></td>
        <td><%= task.complete%></td>
        <td><%= task.due%></td>
        <td><%= link_to 'Show ', task, class:"btn btn-success btn-large, col-md-offset-6" %> </td>
        <td><%= link_to 'Edit ', edit_task_path(task), class:"btn btn-info btn-large, col-md-offset-4" %></td>
        <td><%= link_to 'Destroy ', task, class:"btn btn-danger btn-large, col-md-offset-1", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<hr></hr>

<br>
