<%= form_for(@users) do |f| %>
  <% if @users.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <form class="form-horizontal" role="form">
    <div class="form-group">
      <label for="first_name" class="form-control, col-sm-2 control-label">First Name</label>
      <div class="col-sm-10">
        <%= f.text_field :first_name, class:"form-control, col-sm-5", type:"text"%>
      </div>
    </div>
  <br></br>

    <div class="form-group">
      <label for="due" class="form-control, col-sm-2 control-label">Last Name</label>
      <div class="col-sm-10">
        <%= f.text_field :last_name, type:"text", class:"form-control, col-sm-5" %>
      </div>
    </div>
    <br></br>

    <div class="form-group">
      <label for="due" class="form-control, col-sm-2 control-label">Email</label>
      <div class="col-sm-10">
        <%= f.email_field :email, type:"email", class:"form-control, col-sm-5" %>
      </div>
    </div>

    <br></br>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit class:"btn btn-primary" %>
      </div>
    </div>
  </form>
<% end %>
